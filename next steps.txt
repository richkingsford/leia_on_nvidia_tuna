This solution has a ton of custom code for our robot, named Leia, which auto-stacks bricks using ~15 steps.  The current solutions are total garbage.  We must replace as many of these garbage solutions with native nvidia solutions. This repo is in a safe sandbox I copied from the old solution - so we can safely hard delete anything here. Let's do this one small step at a time - until we can confidently stack bricks with Leia. 





ros2_control DiffDriveController — replaces raw Arduino PWM with proper velocity control, which makes the gap-closing math much simpler and more reliable
A PID node (from control_toolbox) — replaces the 5-band speed-score lookup tables with a real closed-loop controller